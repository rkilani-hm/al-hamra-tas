# M2.2 — MenaME (MenaITech) HRMS Integration (dormant) (Spec)

## Objective
Complete the MenaME HRMS integration surface. The dormant adapter + handoff were built in M1.11 (`tas_hrms_adapter_config`, `handoff_to_mename`); M2.2 adds the integration status/monitoring surface (adapter status + queued-handoff count) and a dormant connection-test, wired into the /app/admin/integrations page. New surface only — reuses `settings.manage`. SYSTEM_ADMIN bypasses. No new table, no new key.

## RPCs (gate settings.manage; EXECUTE authenticated)
- `mename_status()` → jsonb — { adapter: {provider,is_enabled,config_status}, queued_handoffs: count of tas_onboarding.handoff_status='queued' }.
- `mename_test_connection()` → jsonb — dormant: if adapter disabled → { ok:false, dormant:true, message }; if enabled → dormant stub { ok:true, dormant:false } (no real HTTP until the MenaITech client is wired); audit.
- Migration: 14-digit ts sorts last; no storage bucket literal; no recursive CTE; idempotent.

## Feature — extend src/features/integrations
- Add mename_status/mename_test to the integrations api.
- Extend `/app/admin/integrations` route with a MenaME (HRMS) card: adapter status + queued-handoff count + enable toggle (reuses set_adapter_enabled hrms) + a "test connection" button showing the dormant result.
- Gate on `settings.manage`; SYSTEM_ADMIN passes.

## ENV
No secrets committed. **Going live needs the MenaITech API base URL + credentials (API key / OAuth) in Supabase/Lovable secrets**, then enable the hrms/mename adapter. Flag in report.

## i18n
Extend `integrations.*` (mename card labels + test) EN/AR (ar key-complete).

## DoD
tsc 0; /app/admin/integrations 200; ar parity; settings.manage-gated; dormant test; interim casts flagged.
