# M2.3 — Microsoft 365 Integration (dormant) (Spec)

## Objective
Unify the Microsoft 365 integration surface: Entra ID (auth — already LIVE via M0.1 sign-in), Outlook email + Teams meetings (M0.4 comm adapter channels, dormant), SharePoint document storage (M0.5 storage adapter, dormant). Provides a single M365 status view + toggles via the admin settings surface. Also FIXES a defect in M3.2's list_adapters/set_adapter_enabled (comm table keys on `channel`, not `provider`). New surface only — reuses `settings.manage`. SYSTEM_ADMIN bypasses.

## Fix (carried defect)
- `tas_comm_adapter_config` keys on **channel** (in_app/outlook_email/teams/sms), not provider. M3.2's list_adapters/set_adapter_enabled referenced `provider` on the comm table → runtime error. CREATE OR REPLACE both to alias `channel` as the provider for the comm kind.

## RPCs (gate settings.manage; EXECUTE authenticated)
- `m365_status()` → jsonb — { entra: live, outlook: {is_enabled,config_status}, teams: {…}, sharepoint: {…} } from the comm/storage adapters.
- CREATE OR REPLACE `list_adapters()` (comm→channel fix) and `set_adapter_enabled()` (comm branch keys on channel).
- No new table, no new key. Migration: 14-digit ts sorts last; no storage bucket literal; no recursive CTE; idempotent.

## Feature — src/features/integrations
- api.ts (interim loose-cast), types.ts.
- Route `/app/admin/integrations` — M365 component status cards (Entra live badge; Outlook/Teams/SharePoint dormant + enable via existing adapter toggle) + a secrets/next-steps note. Admin-only sidebar entry.
- Gate on `settings.manage`; SYSTEM_ADMIN passes.

## ENV
No secrets committed. **Going live needs an Entra app registration with Microsoft Graph delegated/app permissions (Mail.Send, OnlineMeetings, Sites.ReadWrite.All) + client secret in Supabase/Lovable secrets**, then enable the relevant adapters. Flag in report.

## i18n
Full `integrations.*` namespace EN/AR (ar key-complete) + nav.integrations.

## DoD
tsc 0; /app/admin/integrations 200; ar parity; comm-channel fix verified; settings.manage-gated; interim casts flagged.
