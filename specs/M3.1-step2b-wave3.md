# M3.1-step2b-wave3 — Enforcement Flip: Interview-score / Offer / Approval (+ offer.view read gate) (Spec)

## Objective
Final enforcement wave. Flip the remaining sensitive writes to data-driven _has_permission(): the offer RPCs, plus the two ASSIGNMENT-SENSITIVE operations (submit_interview_score, act_on_task) which need permission key AND assignment check. Also add ONE new permission key offer.view and gate offer READS on it (salary sensitivity — the one read-gate in this wave, per the agreed measured-read-gating decision). Completes M3.1-step2b. SYSTEM_ADMIN bypasses all (via _has_permission).

## CRITICAL — read each RPC's REAL current definition + guard before editing
CREATE OR REPLACE on existing RPCs (Lovable-reauthor-risk surface). For EACH: read current live definition from its source migration (use the CURRENT definition — account for any realign/later migration that superseded the original), reproduce VERBATIM, change ONLY authorization, keep signature/body/SECURITY DEFINER/search_path identical. Report each RPC's current-guard category (A service-role-revoked / B authenticated-ungated) — treatment differs:
- Cat A (revoked from public): add gate + `grant execute ... to authenticated`.
- Cat B (granted authenticated): add gate only.
The offer RPCs (M1.9) are likely Cat A (service-role-guarded, matching M1.2 posture) — VERIFY. submit_interview_score (M1.7) and act_on_task (M0.3) are likely Cat B — VERIFY.

## The two ASSIGNMENT-SENSITIVE operations (the crux of this wave)
These already contain assignee/panelist logic — PRESERVE it, ADD the permission key (defense in depth: permission = "this role may do this at all"; assignment = "but only your own"):
- submit_interview_score (M1.7): currently checks the caller is a PANELIST on that interview (or the dormant/service path). KEEP that panelist check exactly. ADD `if not _has_permission('interview.score') then raise 'not_authorized'; end if;`. Both must pass: hold interview.score AND be a panelist.
- act_on_task (M0.3 workflow): currently checks the caller is the task ASSIGNEE (or valid delegate). KEEP that assignee/delegate check exactly. ADD `if not _has_permission('approval.act') then raise 'not_authorized'; end if;`. Both must pass: hold approval.act AND be the assignee/delegate.
Read both current bodies carefully; the permission gate goes alongside (not replacing) the assignment logic. Report how each currently resolves the caller + assignment so the gate is placed correctly.

## Permission key mapping (wave 3)
- offer draft create/edit (if RPC; if direct-table, UI-gate + note) → offer.write
- submit_offer → offer.submit
- issue_offer → offer.issue
- respond_to_offer → offer.respond
- transition_offer (cancel/expire) → offer.write
- submit_interview_score → interview.score (+ panelist check preserved)
- act_on_task → approval.act (+ assignee/delegate check preserved)
- offer_detail, list_offers (READS) → offer.view  [NEW key — the read gate]

## New permission key: offer.view
- Seed offer.view into tas_permission (area 'offer', bilingual name e.g. "View Offers"/"عرض العروض", is_system false) — idempotent, ON CONFLICT on the REAL unique (key) WHERE key is not null (partial — repeat predicate, rubric 12).
- Seed tas_role_permission grants for offer.view to: SYSTEM_ADMIN, HR_MANAGER, RECRUITER, HIRING_MANAGER (the roles that may see salary). NOT to APPROVER/INTERVIEWER/AUDITOR. Wait — AUDITOR: AUDITOR has read-only/audit posture; decide: AUDITOR should arguably SEE offers for audit but not write. GRANT offer.view to AUDITOR as well (read-only auditor sees everything). So: SYSTEM_ADMIN, HR_MANAGER, RECRUITER, HIRING_MANAGER, AUDITOR get offer.view; APPROVER/INTERVIEWER do not. (Idempotent ON CONFLICT (role_id, permission_id) DO NOTHING.)
- Resolve role ids by code, permission id by key.

## Decisions (locked)
- Gate/grant change inside existing RPCs; reproduce verbatim otherwise. One new key (offer.view) + its grants seeded. No new tables, no other new keys.
- Offer READS gated on offer.view (offer_detail, list_offers): add `if not _has_permission('offer.view') then raise 'not_authorized'; end if;` (these are reads but salary-sensitive — the agreed exception). Verify their current guard category for grant handling.
- Assignment-sensitive ops: permission key ADDED, assignment check PRESERVED — both required.
- SYSTEM_ADMIN bypass intact.
- UI gating on useAuth().capabilities:
  - offer create/submit/issue/respond/cancel controls → matching offer.* keys
  - offer screens/OfferPanel visibility/read → offer.view (hide offer panel/list/detail content when lacking offer.view; SYSTEM_ADMIN passes)
  - interview scorecard submit control → interview.score (the panelist check still applies server-side)
  - approval task act buttons → approval.act (assignee check still applies server-side)
  loading caps → disabled/hidden, no flash.
- Reads OTHER than offers stay open (no other read-gating this wave).
- Do NOT touch config/requisition/application/candidate/screening enforcement (waves 1+2 done).

## Requirements

### Migration — supabase/migrations/<14-digit ts after all existing>_m3_1_step2b_wave3_offer_approval_enforce.sql
(Timestamp after the latest existing migration — check supabase/migrations/, sort after it.)
- Seed offer.view permission + grants (as above), idempotent, rubric-12-correct ON CONFLICT.
- Reproduce + re-gate each target RPC VERBATIM (auth-only change):
  - Offer writes (verify Cat A/B; likely A → gate + grant): submit_offer (offer.submit), issue_offer (offer.issue), respond_to_offer (offer.respond), transition_offer (offer.write). (offer draft create/edit: if it's an RPC, gate offer.write; if direct own-draft table insert like requisition, UI-gate only + report.)
  - Offer reads (verify Cat A/B): offer_detail (offer.view), list_offers (offer.view).
  - submit_interview_score (Cat B likely): ADD interview.score gate, PRESERVE panelist check.
  - act_on_task (Cat B likely): ADD approval.act gate, PRESERVE assignee/delegate check.
- Cat A RPCs: add gate + EXECUTE-to-authenticated grant. Cat B: gate only.
- Gate placement: after caller resolution, before the operation; for assignment ops, alongside the existing assignment check (both must pass).
- Idempotent (CREATE OR REPLACE). No storage[space]buckets literal (even in comments — rubric 4). ON CONFLICT on offer.view seed matches the partial key index (repeat WHERE predicate). Reproduce any existing ON CONFLICT in the RPC bodies unchanged. Recursive CTEs reproduced as-is. Verify all signatures UNCHANGED post-reproduction.

### Feature wiring — capability gating
- Offer components (OfferForm, OfferDetail, OfferPanel, OfferLetterPreview, offers list): gate write controls on offer.write/submit/issue/respond; gate the offer READ surface (panel content, list, detail) on offer.view (hide/empty-state when lacking it). SYSTEM_ADMIN passes (all keys).
- Interview scorecard submit control (InterviewScorecardForm / wherever submit_interview_score is called): gate on interview.score.
- Approval act buttons (workflow inbox / task act UI): gate on approval.act.
- useAuth().capabilities; loading → disabled/hidden, no flash. not_authorized backstop toast (bilingual).
- Confirm no api.ts signature change (RPCs same signatures) — likely no typed-swap; if offer.view gate on reads changes a return contract (it shouldn't — same shape, just may raise), no swap.

### i18n
offer.view needs UI labels only if a new gating/empty-state message is shown (e.g. "You don't have permission to view offers"). Add minimal keys to en.json + ar.json if needed (ar key-complete). Permission NAME for offer.view is seeded data (en/ar in the seed). Otherwise reuse not_authorized.

### ENV
No new secrets, no edge functions, no storage bucket.

## Edge cases
- Per-RPC guard category verified by reading real source — report A/B per RPC. Cat A offer RPCs MUST get the EXECUTE grant or permitted users can't call them.
- Assignment ops: BOTH checks required. A user with interview.score who is NOT a panelist → denied (panelist check). A panelist WITHOUT interview.score → denied (permission). Same logic for act_on_task (approval.act + assignee). Confirm both directions.
- offer.view read gate: a user without offer.view calling offer_detail/list_offers → not_authorized; UI hides the offer surface. RECRUITER/HR_MANAGER/HIRING_MANAGER/AUDITOR/SYSTEM_ADMIN have it.
- respond_to_offer is recruiter-recorded (MVP) → offer.respond held by RECRUITER/HR_MANAGER (verify seeded grants include it; if not, note — RECRUITER may need offer.respond).
- SYSTEM_ADMIN: everything works (bypass) — regression.
- capabilities loading: disabled/hidden, no flash.
- Reproduce verbatim — verify signatures unchanged post-apply.

## Definition of done
- tsc --noEmit exit 0; routes (all) 200; ar.json key-complete.
- Migration sorts last; offer.view seeded + granted (SYSTEM_ADMIN/HR_MANAGER/RECRUITER/HIRING_MANAGER/AUDITOR); ON CONFLICT rubric-12-correct; no storage[space]buckets literal.
- Each target RPC reproduced verbatim, auth-only change; Cat-A get EXECUTE grant; signatures unchanged; per-RPC A/B reported.
- Assignment-sensitive ops: interview.score + panelist check both enforced on submit_interview_score; approval.act + assignee/delegate check both enforced on act_on_task — assignment logic PRESERVED.
- Offer reads gated on offer.view; offer writes gated on offer.*; UI gated accordingly (read surface hidden without offer.view).
- SYSTEM_ADMIN unchanged (regression); correct roles allowed/denied per the mapping.
- No wave-1/2 module enforcement changed; one new key only.

## Verification plan (post-apply)
- Signatures UNCHANGED (pg_get_function_identity_arguments) for all reproduced RPCs.
- Cat-A offer RPCs have EXECUTE to authenticated.
- offer.view seeded (1 new perm) + granted to the 5 roles; APPROVER/INTERVIEWER lack it.
- DB-level grant resolution (SYSTEM_ADMIN bypasses, so this is the proof): for offer.write/submit/issue/respond → which roles (HR_MANAGER + RECRUITER for write/submit/respond; offer.issue HR_MANAGER per seed); interview.score → INTERVIEWER + RECRUITER + HR_MANAGER per seed; approval.act → APPROVER + HIRING_MANAGER + HR_MANAGER per seed; offer.view → the 5 roles.
- Assignment logic intact: (best via a non-admin sign-in, else inspect the RPC bodies confirm panelist/assignee checks remain).
- Regression: as SYSTEM_ADMIN, offer create→submit→issue→respond + an interview score + an approval act all still work.
